import ReactDOM from 'react-dom/client'
import App from './App'
// Import the css stylesheets
import './index.css'

// React is hooking the app with html element id root 
ReactDOM.createRoot(document.getElementById('root')).render(<App />)

// Select App  then press ctrl + space to automatically import App